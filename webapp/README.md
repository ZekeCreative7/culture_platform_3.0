# Culture Platform 3.0 Web App

This is the browser-first replacement for the original Streamlit prototype.

## Run locally

```bash
cd webapp
python3 -m http.server 4173
```

Open `http://localhost:4173`.

## Deploy

Deploy the `webapp` directory as a static site on Vercel, Netlify, or GitHub Pages.
